/**
 * Temporary renderer-only visibility switches.
 *
 * Proxy services and stored configuration intentionally remain enabled so the
 * UI can be restored without rebuilding the underlying feature.
 */
export const SHOW_INVOICE_PROXY_CONTROLS = false;
